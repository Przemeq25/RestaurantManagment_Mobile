import styled from 'styled-components';

export const StyledScrollWrapper = styled.ScrollView.attrs(() => ({
  contentContainerStyle: {
    minHeight: '100%',
    paddingLeft: 10,
    paddingRight: 10,
    backgroundColor: '#F8F8F8',
  },
}))``;
